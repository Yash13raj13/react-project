import React from "react";

const Navbar: React.FC = () => {
  return (
    <nav style={{ padding: "10px", background: "#111", color: "white" }}>
      <h2>Music Theory App</h2>
    </nav>
  );
};

export default Navbar;